---
id: dashboard-按任务找工具
type: dashboard
area: 库
status: active
tags: [dataview, 查询, 任务选型]
title: 按任务找工具 · 任务名 → 推荐列表
summary: Dataview 动态查询——输入任务名关键词，按 tier 排序输出匹配工具列表。
created: 2026-07-31
updated: 2026-07-31
related:
  - tools/工具选型矩阵.md
  - tools/任务选型速查.md
see_also:
  - tools/标签规范.md
---

> 本页依赖 [Dataview](https://github.com/blacksmithgu/obsidian-dataview) 插件，需在 Obsidian 中打开才能渲染。
> 静态版入口见 [任务选型速查](任务选型速查.md)。

# 按任务找工具

> 下方每个任务标签对应一个 Dataview 查询块，**按 tier 排序**（S > A > B > C），同 tier 内按 stars 降序。
> 在 Obsidian 中打开本页即可看到实时结果。

---

## 去 AI 味 / 降 AI 检测

```dataview
TABLE
  tier AS "评级",
  file.link AS "工具名",
  stars AS "⭐",
  use_case AS "使用场景"
FROM "tools/cards"
WHERE contains(tags, "去AI味")
SORT tier ASC, stars DESC
```

## 大纲规划 / 搭框架

```dataview
TABLE
  tier AS "评级",
  file.link AS "工具名",
  stars AS "⭐",
  use_case AS "使用场景"
FROM "tools/cards"
WHERE contains(tags, "大纲规划")
SORT tier ASC, stars DESC
LIMIT 50
```

## 人物设定 / 角色设计

```dataview
TABLE
  tier AS "评级",
  file.link AS "工具名",
  stars AS "⭐",
  use_case AS "使用场景"
FROM "tools/cards"
WHERE contains(tags, "人物设定")
SORT tier ASC, stars DESC
```

## 改稿润色 / 校对

```dataview
TABLE
  tier AS "评级",
  file.link AS "工具名",
  stars AS "⭐",
  use_case AS "使用场景"
FROM "tools/cards"
WHERE contains(tags, "改稿润色") OR contains(tags, "校对")
SORT tier ASC, stars DESC
```

## 多 Agent 协作 / 自动产文

```dataview
TABLE
  tier AS "评级",
  file.link AS "工具名",
  stars AS "⭐",
  use_case AS "使用场景"
FROM "tools/cards"
WHERE contains(tags, "多Agent")
SORT tier ASC, stars DESC
LIMIT 50
```

## RAG 记忆 / 长文一致性

```dataview
TABLE
  tier AS "评级",
  file.link AS "工具名",
  stars AS "⭐",
  use_case AS "使用场景"
FROM "tools/cards"
WHERE contains(tags, "RAG")
SORT tier ASC, stars DESC
```

## TTS 语音 / 有声化

```dataview
TABLE
  tier AS "评级",
  file.link AS "工具名",
  stars AS "⭐",
  use_case AS "使用场景"
FROM "tools/cards"
WHERE contains(tags, "TTS")
SORT tier ASC, stars DESC
```

## 本地写作 / 离线工具

```dataview
TABLE
  tier AS "评级",
  file.link AS "工具名",
  stars AS "⭐",
  use_case AS "使用场景"
FROM "tools/cards"
WHERE contains(tags, "本地写作") AND contains(tags, "本地优先")
SORT tier ASC, stars DESC
LIMIT 50
```

## 平台运营 / 投稿数据

```dataview
TABLE
  tier AS "评级",
  file.link AS "工具名",
  stars AS "⭐",
  use_case AS "使用场景"
FROM "tools/cards"
WHERE contains(tags, "平台运营")
SORT tier ASC, stars DESC
LIMIT 50
```

## 灵感创意 / 脑暴发想

```dataview
TABLE
  tier AS "评级",
  file.link AS "工具名",
  stars AS "⭐",
  use_case AS "使用场景"
FROM "tools/cards"
WHERE contains(tags, "灵感创意")
SORT tier ASC, stars DESC
LIMIT 50
```

---

> 当前标签体系：受控 22 个能力标签 + 开放技术语言 | 共 3574 张工具卡
> 标签体系说明见 [标签规范](标签规范.md)