---
id: tool-07284
type: tool
area: 库
status: active
tags: [TTS, HTML, 协议未明, 本地优先, 中文友好, 本地写作]
title: fxdownloader
summary: 小说转语音/有声书
source: https://github.com/halei0v0/fxdownloader
created: 2026-07-18
updated: 2026-07-18
no: 7284
category: 画龙补充 / 扩容入库 — 补充源
repo: halei0v0/fxdownloader
stars: 8
url: https://github.com/halei0v0/fxdownloader
tier: "B"
use_case: "小说转语音/有声书"
pitfalls:
  - "⚠️ 协议未声明，商用/分发前务必到仓库确认授权"
related:
  - methods/QUICK_START.md
---

# halei0v0/fxdownloader

- **分类**：画龙补充 / 扩容入库 — 补充源
- **链接**：https://github.com/halei0v0/fxdownloader
- **Stars**：8
- **语言**：HTML
- **License**：None
- **Topics**：—
- **GitHub 描述**：好用的小说爬虫工具，下载你想要的所有~
- **本地描述**：fxdownloader
- **拉取时间**：2026-07-25 19:16:37

---

# FXdownloader

多源小说下载工具，支持蚂蚁文学、思兔阅读、顶点小说、笔下文学、铅笔小说、海棠文学、番茄小说七大源，多源自动搜索、相关性排序、分工加速下载、三层失败重试、缺失内容自动补全。

【项目使用AI编写不建议二次开发（如果你愿意看的话(●ˇ∀ˇ●)）感谢[Scrapling](https://github.com/D4Vinci/Scrapling)爬虫提供的参考和思路】

## 功能特点

### 核心下载

- **多源聚合搜索** - 输入书名即可同时搜索七大源，无需手动切换，自动给出所有可用结果
- **相关性排序** - 搜索结果按标题相关性自动排序：完全匹配 > 前缀匹配 > 包含匹配，同分时主源优先、有封面优先
- **多源分工下载** - 主源下载失败时自动切换其他源重试，按章节标题跨源匹配
- **三层失败重试** - 单章失败时先在相同源重试（3 次退避）→ 跨源重试（按标题匹配）→ 缺失补缺，最大化下载成功率
- **失败章节自动选中** - 所有重试仍失败的章节会自动勾选并以红色高亮，滚动定位到首个失败章节，方便手动重试
- **章节去重排序** - 自动剔除章节列表开头倒序的"最新章节"，正确排序
- **字体解密** - 自动解密番茄小说的字体加密内容
- **批量下载** - 支持选择章节范围批量下载，并发加速

### 发现与浏览

- **排行榜** - 从速读谷 (sudugu.org) 获取真实热门排行榜（前 30 名，3 页），按天缓存避免反爬
- **分类浏览** - 7 大分类（玄幻/仙侠/都市/历史/军事/科幻/言情），数据同样来自速读谷并按天缓存
- **点击即搜** - 排行榜和分类中的小说点击后，自动用书名在各源搜索可用结果
- **封面缓存** - 小说封面获取一次后自动保存到本地，搜索结果、收藏列表均自动显示封面，后续不再重复请求
- **封面缓存管理** - 设置面板显示封面缓存条目数和占用大小，可一键清除
- **小说状态显示** - 搜索结果卡片和详情页均显示完本/连载状态标签（绿色完本/橙色连载），自动从源解析并统一映射

### 下载管理

- **下载进度预估** - 实时显示下载进度、速度和剩余时间预估
- **暂停/续传** - 支持随时暂停下载任务，之后从断点续传，不重复下载已完成章节
- **章节更新** - 一键检查并下载小说的新增章节，同时检测并补全上次未完成的内容
- **历史记录** - 自动记录下载历史，可快速重新打开
- **收藏夹** - 收藏喜爱的小说，卡片支持直接点击"阅读"按钮进入阅读器
- **阅读功能** - 内置阅读器，支持连续滚动阅读（章末自动加载下一章，同页面无缝衔接）

### 阅读体验

- **连续滚动阅读** - 阅读到章节末尾时自动加载下一章并追加到当前页面，无需跳转等待
- **语音朗读 (TTS)** - 基于 edge-tts 在线语音合成，朗读当前章节内容，支持语速调节（0.5x-2x）、14 种中文音色、当前句高亮、自动滚动、读完自动接下一章
- **角色识别朗读** - 自动解析对话中的说话人，旁白使用云希男声，角色按性别自动分配不冲突的音色（男声池/女声池轮换），无主语对话延续上一说话人，鼠标悬停显示角色名
- **护眼背景** - 默认开启护眼模式（暖色纸质感），可一键切换深色背景
- **字号自动保存** - 字号范围 12-36 px 自由调节，调整后自动保存到配置，下次打开自动恢复
- **阅读进度记忆** - 自动记录最后阅读位置，下次打开直接定位到上次章节
- **章节快速跳转** - 侧栏章节列表实时高亮当前章节，点击即跳转

### 界面

- **现代化 WebUI** - 基于 pywebview + HTML/CSS/JS 的深色主题界面
- **初始发现页** - 启动即显示排行榜和分类，点击小说后平滑切换到详情视图
- **购物篮批量下载** - 将多本小说加入购物篮，一键批量下载
- **应用图标** - 支持自定义应用图标（打包 EXE 与开发模式窗口均显示），兼容新旧版本 pywebview
- **EXE 打包** - 支持 PyInstaller 一键打包为独立可执行文件
- **开发数据清理** - 内置清理脚本，一键清除数据库/下载文件/缓存/配置等开发测试数据

## 支持的小说源

| 源 | 域名 | 特点 |
|---|---|related:
  - methods/QUICK_START.md
---|
| 蚂蚁文学 | mayiwsk.com | 无需登录，支持搜索 |
| 思兔阅读 | sto66.com | 无需登录，支持搜索，章节列表分页 |
| 顶点小说 | 23wxx.net | 无需登录，og:meta 完整 |
| 笔下文学 | bxwxber.cc | 无需登录，GBK 编码 |
| 铅笔小说 | 23qb.net | 无需登录，支持搜索，章节列表在独立 catalog 页 |
| 海棠文学 | htwenxe.com | 杰奇 CMS，部分章节需登录 |
| 番茄小说 | fanqienovel.com | 需登录，支持字体解密 |

## 环境要求

- Python 3.8+
- Windows / Linux / macOS
- Edge 浏览器驱动（仅番茄官网登录需要）

## 安装步骤

1. 克隆项目：
```bash
git clone https://github.com/halei0v0/FXdownloader.git
cd FXdownloader
```

2. 安装依赖：
```bash
pip install -r requirements.txt
```

3. 安装 Scrapling（可选，用于多源爬取，推荐 editable 安装）：
```bash
pip install -e Scrapling-main
```

## 使用方法

### WebUI 模式（推荐）

**Windows：** 双击 `启动WebUI.bat`

**命令行：**
```bash
python web_ui/app.py
```

打开后自动启动本地服务器并弹出窗口。界面操作：

1. **发现页** - 启动后显示排行榜和分类，点击小说进入详情
2. **搜索** - 顶部输入书名或小说 URL，点击搜索，结果聚合所有源
3. **下载** - 选择小说后获取章节列表，勾选要下载的章节（默认全选）
4. **更新小说** - 点击"检查更新"按钮，自动检测新增章节和缺失内容，仅选中需要下载的章节
5. **管理** - 通过左侧导航访问历史记录、收藏夹、暂停任务
6. **批量** - 将多本小说加入购物篮，一键批量下载

### GUI 模式（旧版 tkinter）

```bash
python gui.py
```

### 命令行模式

```bash
# 通过 URL 下载
python main.py download https://fanqienovel.com/page/711914860

# 通过小说 ID 下载
python main.py download 711914860

# 下载指定章节范围
python main.py download 711914860 --start 1 --end 50

# 搜索小说
python main.py search 诡秘之主

# 列出已下载的小说
python main.py list

# 导出已下载的小说
python main.py export 711914860
```

### 打包为 EXE

**Windows：** 双击 `打包.bat`

**命令行：**
```bash
pyinstaller FXdownloader.spec
```

打包后的可执行文件位于 `dist/FXdownloader.exe`。

## 项目结构

```
FXdownloader/
├── web_ui/
│   ├── app.py              # WebUI 后端（pywebview 桥接）
│   └── index.html          # WebUI 前端（HTML/CSS/JS）
├── sources/
│   ├── __init__.py         # 源注册表
│   ├── base.py             # 源抽象基类
│   ├── biquge_source.py    # 蚂蚁文学源
│   ├── sto66_source.py    # 思兔阅读源（章节列表分页）
│   ├── fanqie_source.py    # 番茄小说源
│   ├── bing_search.py      # Bing 搜索（补充搜索方式）
│   ├── sudugu_rankings.py  # 速读谷排行榜+分类（按天缓存，3 页 30 本）
│   └── multi_source.py     # 多源搜索 + 相关性排序 + 分工下载管理
├── assets/
│   └── icon.ico            # 应用图标（6 分辨率，16-256px）
├── config.py               # 配置管理
├── database.py             # SQLite 数据库（WAL 模式，支持并发）
├── downloader.py           # 下载器核心
├── spider.py               # 番茄小说爬虫
├── font_decrypt.py         # 字体解密
├── selenium_login.py        # Selenium 自动登录
├── gui.py                  # 旧版 tkinter GUI
├── main.py                 # 命令行入口
├── FXdownloader.spec       # PyInstaller 打包配置
├── Scrapling-main/         # Scrapling 爬虫库源码
├── 启动WebUI.bat           # WebUI 启动脚本
├── 启动.bat                # 旧版 GUI 启动脚本
├── 启动命令行.bat          # 命令行启动脚本
├── 打包.bat                # 打包脚本
├── 清理开发数据.bat        # 开发数据清理脚本
└── requirements.txt        # 依赖列表
```

## 依赖项

- **requests** - HTTP 请求库
- **beautifulsoup4** - HTML 解析
- **lxml** - XML/HTML 解析引擎
- **pywebview** - WebUI 窗口容器
- **scrapling** - 高级爬虫框架（多源爬取）
- **curl_cffi** - TLS 指纹模拟（scrapling 依赖）
- **selenium** - 浏览器自动化（番茄登录）
- **fonttools** - 字体处理
- **Pillow** - 图像处理
- **ddddocr** - 验证码识别
- **fake-useragent** - 随机 User-Agent
- **edge-tts** - 微软 Edge 在线语音合成（朗读功能）

## 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

## 免责声明

本工具仅供个人学习和研究使用，请勿用于商业用途。使用本工具下载的内容版权归原作者所有，请在下载后 24 小时内删除。

## 注意事项

- 请遵守相关法律法规，尊重版权
- 请勿频繁请求，避免对服务器造成压力
- 番茄官网模式下载需要登录，请使用 Selenium 自动登录或手动添加 Cookie
- 番茄官网可能出现人机验证，触发后需等待几小时恢复
- 如需使用自动登录，请下载对应版本的 msedgedriver.exe
- 多源搜索依赖各源自身搜索接口，部分源搜索不可用时会影响结果数量
- 排行榜和分类数据来自速读谷 (sudugu.org)，每日只请求一次后缓存到本地
- 失败章节重试时自动以追加模式写入已有文件，不会覆盖已下载内容

## 贡献

欢迎提交 Issue 和 Pull Request！

## 作者

halei0v0

## 更新日志

### v2.1.4

**新增功能**

- 语音朗读 (TTS)：基于 edge-tts 在线语音合成，在阅读器中点击"朗读"按钮即可朗读当前章节，后端生成 mp3 并 base64 传输至前端播放，支持播放/暂停/停止、上一句/下一句跳转、语速调节（0.5x-2x）、14 种中文音色选择、读完本章自动接下一章
- 角色识别朗读：自动解析段落中的对话与旁白，旁白统一使用云希男声（zh-CN-YunxiNeural），对话角色按性别自动分配不冲突的音色（男声池云健/云扬轮换，女声池 13 个音色依次分配），无主语对话延续上一说话人，朗读时鼠标悬停当前句显示角色名
- 当前句高亮与自动滚动：朗读时当前句黄色高亮（护眼模式适配暖色），自动滚动到视口中央

**优化改进**

- 朗读单元切分：以整个段落为朗读单元（不再按句号细切），避免句间停顿不连贯；超过 150 字的长段按逗号/顿号二次切分，仍超长则按 150 字硬切分，避免 edge-tts 超时
- 音频预获取机制：播放当前句时后台预获取下一句音频并缓存，下一句播放时直接命中缓存，显著减少句间停顿
- 请求去重：同一句的预获取与正式请求复用 Promise（`_ttsInflight`），避免重复调用后端
- 音频内存释放：跳转/停止/切句时清空 Audio 元素 src 触发 GC，避免内存泄漏
- 朗读进度显示：状态栏显示"朗读中 (3/25)"格式，直观看到当前句子进度
- 生成失败自动跳过：单句生成失败 200ms 后自动跳到下一句，不中断整体朗读
- 引号字符清理：后端生成前清理中文引号 `""`、单引号 `''`、日文引号 `「」『』`、书名号 `《》` 等 edge-tts 无法合成的字符
- 朗读控制条 UI 与阅读器风格统一：按钮、下拉框、状态文字使用与护眼/字号按钮一致的 `bg-glass` + `border-glass` 风格，护眼模式下全部适配暖色调
- 阅读器导航按钮（关闭/上一章/下一章）尺寸与护眼/朗读按钮统一
- 令牌机制（genToken）：跳转/暂停/停止时自增，丢弃过期的生成请求，避免旧音频覆盖新状态

**Bug 修复**

- 修复引号导致 TTS 生成失败：edge-tts 对中文引号等特殊字符无法合成返回空音频报错 "No audio was received"，后端在生成前清理这些字符
- 修复说话人名字贪婪匹配：`{1,8}` 贪婪量词导致 `林云说道` 被识别为 speaker=`林云说`（多吃动词字），改为非贪婪 `{1,8}?`
- 修复复合动词"回答"被拆分：`她回答。` 被识别为 speaker=`她回`（"回"当名字，"答"当动词），在动词表开头加入 `回答` 优先匹配长串

### v2.1.3

**新增功能**

- 新增思兔阅读源 (sto66.com)：支持搜索，无需登录，章节列表自动分页抓取（每页 500 章，自动检测"下一页"），书籍信息解析作者/分类/字数/状态/封面
- 搜索结果相关性排序：最准确的书名排在首位（完全匹配 > 前缀匹配 > 包含匹配 > 其他），同分时有封面优先、主源优先
- 小说状态显示：搜索结果卡片右上角显示"完本"（绿色）/"连载"（橙色）状态标签，详情页标签同步显示
- 状态字段统一映射：前端 `formatNovelStatus()` 将各种状态文案（完结/已完结/全本 → 完本；连载中/更新中 → 连载）归一化显示，正确处理"未完结"/"未完本"否定前缀避免误判

**优化改进**

- 后端 `_novel_info_to_dict` 和 `search_all_sources` 将 `status` 从 `extra` 提取到顶层字段，便于前端统一访问
- sto66 源 URL 解析使用 `re.search` 而非 `re.match`，兼容绝对 URL
- sto66 源状态 class 检测支持多 class（`'red' in cls.split()` 而非精确匹配）
- 多源搜索并发调用七大源（蚂蚁文学、思兔阅读、顶点小说、笔下文学、铅笔小说、海棠文学、番茄小说）
- 前端源下拉框新增"思兔阅读"选项，与后端 `list_sources()` 顺序保持一致
- 排行榜和分类页不显示小说状态标签：因速读谷 (sudugu.org) 缓存的状态可能与实际小说源不一致，改为只在点击小说进入详情页后显示从实际源获取的准确状态（`buildNovelCard` 新增 `options.hideStatus` 参数）

**Bug 修复**

- 修复章节列表获取不全的问题：原 `_dedup_and_sort_chapters` 先去重后剔除倒序段，导致同时出现在倒序段和正序主体中的最新章节被永久丢失（蚂蚁文学《末日生存方案供应商》丢失最新 9 章）。改为先检测并跳过倒序段，再对正序主体去重
- 修复思兔阅读源"未知源"错误：前端源下拉框 `<select id="sourceSelect">` 缺少 `<option value="sto66">`，导致 `selectNovel()` 中 `sourceSelect.value = 'sto66'` 静默失败，`loadChapters()` 读取到错误的源 key 传给后端，返回"未知源"。补全下拉框选项后修复
- 修复状态标签误判：`formatNovelStatus` 用子串匹配且 `finishKeywords` 先检查，导致"未完结"/"未完本"包含"完结"/"完本"子串被误判为"完本"。改为优先检查否定前缀关键词

### v2.1.2

**新增功能**

- 连续滚动阅读：阅读到章节末尾时自动加载下一章并追加到当前页面，无需跳转等待，滚动位置实时更新当前章节高亮和阅读进度
- 护眼阅读背景：默认开启护眼模式（暖色纸质感 #F5ECD7），可一键切换深色背景，状态自动保存
- 字号自动保存：字号范围扩大至 12-36 px，调整后自动写入 `config.json`，下次打开自动恢复
- 收藏卡片阅读按钮：收藏列表的小说卡片新增"阅读"按钮，可直接点击进入阅读器，无需先选择小说
- 排行榜多页爬取：扩展为 3 页共 30 本（第 11-20 名在第 2 页 `/paihang/2.html`，第 21-30 名在第 3 页 `/paihang/3.html`），按 `rank_offset` 计算全局排名
- 封面缓存管理：设置面板新增"封面信息缓存"区块，显示缓存条目数和占用大小（KB/MB），可一键清除
- 应用图标：支持自定义应用图标（`assets/icon.ico`，6 分辨率 16-256px），打包 EXE 与开发模式窗口均显示，兼容新旧版本 pywebview（旧版自动回退系统默认图标）
- 开发数据清理脚本：新增 `清理开发数据.bat`，菜单式清理数据库/下载文件/Python 缓存/配置文件

**优化改进**

- 搜索结果和收藏列表自动显示封面：新增 `prefetchAndApplyCovers()` 前端函数，调用后端批量预获取封面 URL 并替换占位符
- 收藏添加时自动从缓存补全封面：`add_favorite()` 在 `cover_url` 为空时自动查询 `novel_covers` 表补全
- 阅读器结构重构为多章节块模式（`.reader-chapter-block`），支持滚动监听 + RAF 节流的自动加载机制
- 侧栏章节列表根据滚动位置实时高亮当前章节
- 上下章按钮已加载则平滑滚动定位，未加载则重置加载

### v2.1.1

**新增功能**

- 同源重试：单章下载失败时先在相同源重试 3 次（带 1s/2s 退避延迟），失败后才进入跨源重试流程
- 失败章节自动选中：所有重试仍失败的章节会自动勾选复选框并以红色高亮，滚动定位到首个失败章节，提示用户手动重试
- 更新小说补全缺失内容：`update_novel` 同时检测新增章节和上次任务中未完成的章节，返回 `incomplete_chapters` 列表并自动选中，下载时以追加模式合并到已有文件

**优化改进**

- 下载流程升级为三层重试：同源重试 → 跨源重试（按标题匹配）→ 缺失补缺
- `download()` 新增 `append_mode` 参数，支持追加写入已有文件（用于补全内容，不覆盖已下载部分）
- 前端新增 `supplementMode` 一次性标志：更新小说或失败重试时自动设置，下载后立即重置，确保追加模式仅在明确补全时使用
- 批量下载中检测到失败章节仅记录日志，不干扰批量流程
- 设置界面移除无效的"下载速度"选项（WebUI 未实际使用，仅保留旧版 GUI 兼容）

### v2.1.0

**新增功能**

- 排行榜：从速读谷 (sudugu.org) 获取真实热门排行，按天缓存到本地数据库，规避反爬
- 分类浏览：7 大分类小说浏览，数据来自速读谷，同样按天缓存
- 点击即搜：排行榜/分类中的小说点击后，自动用书名在各源搜索可用结果
- 封面缓存：小说封面获取一次后保存到本地，后续不再重复请求
- 下载进度预估：实时显示进度、速度和剩余时间
- 暂停/续传：支持随时暂停，从断点续传，不重复下载已完成章节
- 章节更新：一键检查并下载小说的新增章节
- 历史记录：自动记录下载历史
- 收藏夹：收藏喜爱的小说
- 阅读功能：内置阅读器
- 购物篮批量下载：多本小说加入购物篮，一键批量下载
- 初始发现页：启动即显示排行榜和分类，点击小说后平滑切换到详情视图
- 章节范围下载：支持指定章节范围下载
- 默认全本下载：选择小说后默认勾选全部章节

**优化改进**

- 数据库启用 WAL 模式 + 线程锁，提升并发写入可靠性
- 搜索结果统一 `source_key` 字段，前后端接口一致性提升
- 番茄小说 URL 自动识别修复（支持 `fanqie_api` / `fanqie_official`）
- 初始窗口尺寸增大至 1100×780，避免按钮扭曲
- UI 初始化时序修复（`pywebviewready` 事件）
- 清理死代码（`_fill_covers_from_cache`、`download_chapter_multisource`）

### v2.0

- 新增 5 个小说源：蚂蚁文学、顶点小说、笔下文学、铅笔小说、海棠文学
- 多源聚合搜索：输入书名同时搜索所有源，无需切换模式
- 多源分工下载：主源失败自动切换其他源，按章节标题跨源匹配重试
- 章节列表去重排序：自动剔除开头倒序的"最新章节"
- 全新 WebUI：pywebview + HTML/CSS/JS 深色主题界面
- 修复打包问题：scrapling、browserforge、apify_fingerprint_datapoints 数据文件完整打包
- 修复 Bing 搜索：改用各源自身搜索为主，Bing 作为补充

### v1.0.6

- 大幅提高第三方下载模式的下载速度
- 10 章并发下载
- 优化官网模式下载时间预估

### v1.0.5

- 第三方下载模式已可用
- 自动选择 API
- 多 API 支持

### v1.0.4

- 修复登录问题，Cookie 验证从 5 个提高到 24 个
- 下载进度条和预估时间
- 设置界面改进，添加"恢复默认设置"按钮
- 下载速度调整（0.5x-2.0x）
- 界面尺寸增大
- 字体解码修复

### v1.0.3

- 动态章节下载速度以减少触发人机验证
- 断点续传功能

### v1.0.2

- 下载历史显示数据库中的小说信息
- 批量导出/下载功能
- 优化下载历史管理界面

### v1.0.1

- 增加账户登录功能
- 支持 SVIP 账号下载全本小说

### v1.0

- 初始版本发布
- 支持 GUI 和 CLI 两种模式
- 内置字体解密机制
