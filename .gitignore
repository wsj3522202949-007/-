# ============================================================================
# 知识库版本控制排除规则
# ============================================================================

# ----------------------------------------------------------------------------
# Local environment / private data (私密配置与环境数据)
# ----------------------------------------------------------------------------
.workbuddy/
.obsidian/
.tools/
# 恢复演练临时克隆目录（从远端还原的演练工作树，不入库）
.recovery-drill-tmp/

# ----------------------------------------------------------------------------
# 操作系统生成的文件
# ----------------------------------------------------------------------------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ----------------------------------------------------------------------------
# 编辑器配置与缓存
# ----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.project
.settings/
*.sublime-project
*.sublime-workspace

# ----------------------------------------------------------------------------
# 维护运行时状态（备份日志、清理状态、季度演练记录等）
# ----------------------------------------------------------------------------
maintenance/state/
*.tmp
*.temp
*.log
*.cache
*.bak
*.backup
*~

# ----------------------------------------------------------------------------
# Node.js / Python 依赖（如果有脚本）
# ----------------------------------------------------------------------------
node_modules/
__pycache__/
*.py[cod]
*$py.class
venv/
env/
.venv

# ----------------------------------------------------------------------------
# 压缩文件与归档（通常不应进入版本控制）
# ----------------------------------------------------------------------------
*.zip
*.tar
*.tar.gz
*.rar
*.7z

# ----------------------------------------------------------------------------
# 大型二进制文件
# ----------------------------------------------------------------------------
*.pdf
*.docx
*.xlsx
*.pptx
*.jpg
*.jpeg
*.png
*.gif
*.mp4
*.mp3
*.wav

# ----------------------------------------------------------------------------
# Obsidian 插件特定
# ----------------------------------------------------------------------------
.obsidian/plugins/
.obsidian/workspace.json
.obsidian/workspace-mobile.json

# ----------------------------------------------------------------------------
# 测试与草稿
# ----------------------------------------------------------------------------
# /test/ /tests/ 只锚定仓库根：根级测试目录不入库。
# tools/ 下的单元测试（tools/scripts/tests/）天然入库，不依赖反向豁免 ——
# CI unit-tests job 依赖它们（test_chapter_policy.py 共 26 用例），历史上
# 曾因豁免不足导致测试从未入库、CI checkout 后找不到文件（exit 2）。
/test/
/tests/
*.test.md
*.draft.md

# ----------------------------------------------------------------------------
# 敏感信息文件
# ----------------------------------------------------------------------------
*.secret
*.private
*password*
*credentials*